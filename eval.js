const script = '2 + 2';
const result = eval(script);
console.log(`The result of evaluating "${script}" is: ${result}`);  

// Be cautious when using eval with untrusted code, as it can pose security risks.
